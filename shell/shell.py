#!/usr/bin/python3

import os
import sys
import res


#method to accept input
def main():
    while True:
        command = input("$ ")
        if command == "exit":
            break
        else if command == "help":
            print("Type 'exit' to exit. That is all.")
        else if cd in command:
            
        else:
            execute(command)



def execute(command):
    
            
